# M2 reconnaissance evidence - 2026-07-17

## Proven increment

The independent secure reconnaissance layer classifies every working face and
edge exactly once. Each record carries:

- working subject ID and reverse source subject IDs;
- curve or surface taxonomy, exact family code, concrete OCCT dynamic type,
  wrapper stack, domains, periodic/degenerate conditions, and trim class;
- an explicit supported, deferred, invalid, or unrecognised state with a named
  strategy or reason.

No `kernel_specific` family becomes supported. Initial logical regions remain
one source-backed face each, with their complete edge boundary; safe merging is
deferred rather than inferred geometrically.

## Fixtures

- processing-disabled cylinder: exactly one cylinder, two planes, complete
  reverse provenance, three logical regions, and no duplicate/unaccounted
  subject;
- processing-disabled box: six planes remain projection-eligible even though at
  least one (and in practice every) coedge lacks a stored p-curve;
- the cylinder wall is supported only when all of its stored p-curve
  representations compose through exact evaluation.

## Status boundary

This starts M2 but does not pass it. Shell/wire/vertex occurrence hierarchy,
the full trim-domain taxonomy, artificial-split region merging, and explicit
adversarial unknown-family fixtures remain open.
