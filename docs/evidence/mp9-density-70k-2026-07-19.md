# MP9 density ~54k polys under fail-closed — 2026-07-19

## Changes
- Curve-position cache under `previewFast` (edge + quantized parameter).
- Large preview: `previewTriangleBudget` capped to 38k, `minimumClosedCurveSegments` ≤ 6, active radial 8, budget scale 0.60.

## Result
```
50692 vertices, 54406 polygons (41570 quads, 12836 tris)
boundaries ~24s; total face phase ~69s
omitDeferred=false
```

Soft target was ~70k; landed slightly under while keeping hole bridges solvable.
