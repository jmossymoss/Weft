# Secure-core milestone ledger

Last updated: 2026-07-18.

This file is the sole authority for secure-core implementation status. Plans,
handoffs, commit messages, and test output are evidence, not status authority.
Agents execute the ordered packages in `agent-execution-playbook.md`.

Status vocabulary: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `FAILED`, `PASSED`.

| Milestone | Status | Gate | Current evidence / next dependency |
|---|---|---|---|
| M0 - baseline and governance | PASSED | Legacy baseline is reproducible, production routing has one owner, and governance/build routes are explicit | Windows clean build, 14/14 strict and MSVC analyzer tests, legacy generator removal (`9ec3f3f`), and single-owner production routing are proven. Linux GCC 15.2 / OCCT 7.9.2 now passes both `linux-gcc` and `linux-gcc-static-analysis` 14/14 with scoped `-fanalyzer` on `weft_core`, documented TU ICE exclusions, and generated-corpus named-refusal reconciliation. See M0 Windows-clean and M0 Linux GCC analysis evidence. Baseline exception debt remains frozen and non-accepted. |
| M1 - immutable source and working B-rep | PASSED | Every working model has a valid repair/correspondence certificate or a named refusal; identity input has an identity certificate | Processing-disabled STEP, snapshot-protected native ASCII B-rep, and processing-disabled IGES import produce topology-isolated conservative working copies. Bounded SameParameter/SameRange, face-adjacency orientation (including multi-body free solids), tolerance-envelope repair, native-unit resolution, copy-on-write rules, meshable bounded sewing, Compatibility valid-copy and heal-lane correspondence, and a product STEP orientation witness are proven. Multi-way Compatibility heal splits and open/non-manifold orientation attempts emit named `import.*` refusals rather than silent incompleteness. `testM1CertificateOrNamedRefusalGate` sweeps corrupt fixtures under both repair profiles. See the secure-import through certificate-or-named-refusal evidence set. |
| M2 - total reconnaissance | PASSED | Every imported entity is classified/accounted exactly once; unsupported geometry is named | Exact occurrence hierarchy accounts compound/compsolid/solid/shell/face/wire/edge/vertex uses and wire coedges without collapsing repeated instances. Source-backed exact family/wrapper/domain/trim records account every edge/face; all 77 generated sources complete reconnaissance over 1,559 subjects. Schema-aligned trim taxonomy, coplanar artificial-split region merge, multidomain deferral, adversarial OtherCurve/OtherSurface refusal, and oracle trim/family slices are proven. See M2 total-reconnaissance, exact-occurrence, and frozen-corpus evidence. |
| M3 - predicates, counts, canonical boundaries | PASSED | Every valid shared edge has one bijective sample sequence and consistent coedge UV uses | Exact count assignment includes fixed recipe constraints, bounded independent chain-sum equalities, and one bounded connected coupled component, with exhaustive-oracle proof and named minimum/parity/conflict/coupling/complexity refusals. Canonical samples, bounded UV uses/lifts, exact topological endpoint normalization, azimuth registration, exact dyadic predicates, non-vacuous trim validation, and real planar face/coedge assembly pass strict/static adversarial batteries. Closed coedge periodic UV closure witnesses and ambiguous/discontinuous lift refusals are proven. Meshing coedges preserve topology wire occurrence identity; ShapeMap face alias collapse refuses by name. Supported line/circle/plane/cylinder critical parameter events merge into edge-owned sequences with non-vacuous coverage and named unsupported-family refusal before CDT/templates. A planar template consumes independent and bounded coupled chain-sum solutions with non-vacuous requested/solved/consumed coverage and deterministic replay. Count/boundary/lift/report digests for box, capped cylinder, and through-hole match across repeated Windows MSVC and Linux GCC runs. Wider coupled-sum classes remain named `interval.*` refusals under BR-011. |
| M4 - certified tessellation floor | IN_PROGRESS | Every valid or conservatively repairable face produces a deterministic boundary-exact intersection-free triangle mesh | Exact planar/hole CDT and the registered full-cylinder wall now compose atomically through `generateSecureMesh`. A planar box, capped cylinder, and connected through-hole body certify with no weld, complete face coverage, and real OBJ/GLB/FBX CLI exports. General curved surfaces, axial interior provenance/refinement, adaptive patch bounds, critical regions, 3D triangle intersections, app routing, Linux determinism, and corpus-wide meshing remain open. |
| M5 - non-vacuous validation | IN_PROGRESS | All required validator coverage is non-zero/complete; twisted-wall and vacuous-provenance witnesses fail before their fixes | The atomic result aggregates repair, reconnaissance, intervals, boundaries, endpoint normalization, trim, CDT/cylinder error bounds, and body certificates. Repair validation now requires non-zero complete per-p-curve coverage and refuses a valid working shape whose certificate is tampered to `checked=0`. The independent topology-account validator rejects empty evidence, duplicate occurrences, missing exact shapes, missing wire coedges, and missing instance roots by name while allowing genuinely absent flat-model assembly classes. Count mismatch, twisted/reflected registration, extra axial samples, chord/normal excess, wrong periodic lift, missing p-curve provenance, and vacuous face coverage also refuse by name. Remaining source-incidence/Euler, general curved bounds, triangle/triangle, and modelling/mesh provenance-vacuity gates remain open. |
| M6 - planar and cylinder templates | IN_PROGRESS | Box, full/partial cylinder, mismatched-frame cylinder, and connected plane/cylinder fixtures pass end to end | Secure core orchestration, CLI export, desktop viewport, and Blender link now pass the box/full-cylinder family with deterministic certified output; the connected through-hole also passes core/CLI. Modelling still aliases the safe floor. Partial cylinders, interior axial rings, independently certified modelling polygons/quads, and mismatched-reference fixtures remain open. |
| M7 - workflow restoration | PASSED | CLI, recipe migration, app, editing, exports, hot reload, and Blender link use `MeshingResult` with no legacy selector | `mesh`, STEP-to-mesh `convert`, secure global `sweep`, app async regeneration/export, audited hot reload, and Blender live link consume certified results with no legacy selector; `cache-check` refuses explicitly. Source-referenced recipe v2 migrates v1, resolves/captures through audited correspondence, applies exact per-edge counts and one certified per-face cylinder `axial` consumer, admits results only through `admitCertifiedMeshingResult`, rejects stale async epochs, and names LOD/density effects in `GenerationReport::namedLodEffects`. Unsupported per-face/manual operations continue to refuse by name (BR-010). See M7 admission, per-face recipe, cache invalidation, and editing/overlays evidence (2026-07-18). |
| M8 - topology expansion | IN_PROGRESS | Each automatic family has isolated, connected, adversarial, and density-sweep proof | Selected release scope in progress: cone + sphere done; torus (WP-090…095), analytic fillet/blend (WP-100…105), then WP-110 gate. Mapped/freeform remain post-M8 named refusals. |
| M9 - distribution | NOT_STARTED | No unresolved prototype-only dependency; clean build/corpus/package gates pass | Requires M8 selected release scope. |

## Baseline exceptions

The eight failures recorded at the M0 baseline are not accepted product
behaviour. They are frozen pre-rewrite evidence so the new core cannot hide or
relabel them:

- five MP9 routing/count assertions;
- flaregun watertightness and folded-polygon assertions;
- foam watertightness.

Any new failure is a regression. Closing an existing failure requires numeric
and visual evidence.
